# P5.1 — Blind IDs (pré-anotação)

**Status:** done  
**Anotação humana:** ainda não iniciada

## Correção

Substituído `question_uid` (vazava `condition`/`repetition`) por `blind_item_id` (`B0001`…) nas folhas dos avaliadores.

## Artefatos

| Path | Público ao avaliador? |
| --- | --- |
| `collected-data/annotation/blind-id-map.csv` | **NÃO** (privado) |
| `collected-data/annotation/annotation-blind-metadata.json` | **NÃO** (`annotation_order_seed`) |
| `collected-data/annotation/calibration-set.csv` | **NÃO** (tem `question_uid`) |
| `calibration-evaluator-{1,2}.csv` | sim (30 itens opacos) |
| `evaluator-{1,2}.csv` | sim (269; limpos; usar só após freeze v1.0) |

`annotation_order_seed` = `piloto-annotation-order-v1-20260923` (somente ordem de anotação).

## Scripts

- `prepare_blind_ids.py`
- `validate_annotation.py`
- `compare_evaluators.py` (chave `blind_item_id`)
