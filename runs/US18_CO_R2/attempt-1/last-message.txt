[NEEDS CLARIFICATION] Should estimator names be trimmed before validation/storage/display, or should leading/trailing spaces be preserved when the name is accepted?

[NEEDS CLARIFICATION] When an active estimator name is already in use within the same session, what exact behavior should occur: block the join with an error, allow a renamed duplicate, or treat it as reconnecting the same estimator?

[NEEDS CLARIFICATION] After a successful join, should the estimator see an intermediate confirmation state, be immediately redirected to the active game page, or both?

[NEEDS CLARIFICATION] For an invalid or unavailable invite URL, should the system expose one generic message or distinguish between invalid invite, unavailable invite, and closed session?